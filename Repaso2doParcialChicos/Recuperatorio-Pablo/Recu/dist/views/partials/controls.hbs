<div class="d-flex flex-row bd-highlight mb-3">
    <div class="font-weight-light" style="width: 8rem;"></div>
    <div class="row my-4 d-flex justify-content-start">
        <div class="col-md-3.2 mx-auto">
            <div class="card text-center">
                <div class="card-header">
                    <h3>Usuarios</h3>
                </div>
                <div class="card-body">
                    <table class="table table-bordered table-striped table-sm">
                        <thead>
                            <th>ID</th>
                            <th>Nombre</th>
                            <th>Password</th>
                            <th>Accion</th>
                        </thead>
                        {{#each users}}
                        <tr>
                            <!--<td> <input type="checkbox" name="usuario" method="post" value={{id}} > </td>-->
                            <td>{{id}}</td>
                            <td> {{nombre}}</td>
                            <td>{{password}}</td>
                            <td><a class="btn btn-danger" href="/user/delete/{{id}}">Eliminar</a></td>
                            {{else}}
                        <tr>
                            <td colspan="4">No hay mas filas</td>
                        </tr>
                        </tr>
                    </table>
                    {{/each}}
                    <!--<button class="btn btn-success">Enviar</button>-->
                </div>
            </div>
        </div>
    </div>
</div>

<form action="../user/procesar" method="post">
    <table class="table table-bordered table-striped table-sm" border=1>
        <theader>
            <th>ID</th>
            <th>Nombre</th>
            <th>password</th>
            <th>Accion</th>
        </theader>
        {{#each users}}
        <tr>
            <td> <input type="checkbox" name="usuario" method="post" value={{id}}> </td>
            <td>{{id}}</td>
            <td><input type="text" name="usuarios" value="{{nombre}}"></td>
            <td>{{password}}</td>
            <td><a class="btn btn-danger" href="/user/delete/{{id}}">Eliminar</a></td>
            {{else}}
        <tr>
            <td colspan="4">No hay mas filas</td>
        </tr>
        </tr>
        {{/each}}
    </table>
    <BR>
    <button class="btn btn-success">Enviar</button>
    <BR>
    <BR>
</form>